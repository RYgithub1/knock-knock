-# messages#index transfer part
-# = render "xxx"
.trans
  .trans__transContent
    .trans__transContent__transContentDoors
      .trans__transContent__transContentDoors__transContentDoorsLink
        = link_to "Open DoorS", "#", class: "doorsLink"
    .trans__transContent__transContentState
      .trans__transContent__transContentState__transContentStateLink
        -# - if user_signed_in? && @about.user_id == current_user.id
        -#   .trans__transContent__transContentState__transContentStateLink__transContentStateLinkBtn
        -#     = link_to "Edit > About ME", edit_about_path(@about.id), method: :get, class: "transBtn transBtnAbout"
        -#     = link_to "Edit > Sign Up", edit_user_registration_path(@about.id), method: :get, class: "transBtn transBtnSign"
        -# - else
        -# .trans__transContent__transContentState__transContentStateLink__transContentStateLinkBtn
        -#   = link_to "About HIM / HER", "#", class: "transBtn transBtnDoor"


%br
%br
%br
%br
%br
%br
%br
%br
%br
%br
-# messages#index part
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

.mess
  .mess__messContent
    -# タイトル＝＝＝＝＝＝＝＝＝＝＝＝＝
    .mess__messContent__messContentTitle
      .mess__messContent__messContentTitle__messContentTitleDoor
        %h1 Knock-Knock Door
    %br
    %br
    -# ペアユーザー＝＝＝＝＝＝＝＝＝＝＝＝
    .mess__messContent__messContentPair
      .mess__messContent__messContentPair__messContentPairYou
        ペアユーザー名 左づめ
        -# = @about.user.name
        -# = @about.user_id.name
      .mess__messContent__messContentPair__messContentPairMe
        カレントユーザー名 右詰め
        = current_user.name
    %br
    %br
    .mess__messContent__messContentChat
      .mess__messContent__messContentChat__messContentChatYou
        左づめ
        相手アイコン
        自分現在地時刻
        相手投稿写真と投稿テキスト
        %br
        - @pair.messages.each do |message|
          -# = message.user.name
          -# = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
          - if message.content.present?
            = message.content
            = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
          = image_tag message.image.url, class: "chatImage", size: "66x66" if message.image.present?

      %br
      .mess__messContent__messContentChat__messContentChatMe
        右づめ
        自分アイコン
        自分現在地時刻
        自分投稿写真と投稿テキスト


    %br
    %br
    -# 送信フォーム＝＝＝＝＝＝＝＝＝＝＝＝＝
    .mess__messContent__messContentForm
      = form_with model: [@pair,@message], class: "messForm", local: true do |f|
        .messFormBoard
          = f.text_field :content, class: "messText", placeholder: " open and talk"
          .messMask
            = f.label :image, class: "messLabel" do
              %i.fas.fa-camera-retro.chatIcon
              = f.file_field :image, class: "messFile"
        .messFormBtn
          = f.submit "Send", class: "messSubmitBtn"

